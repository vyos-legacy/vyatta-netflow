type: u32

default: 60

help: Expiry scan interval

syntax:expression: $VAR(@) >= 0 && $VAR(@) <= 2147483647; \
       "Timeout timer must be between 0 and 2147483647."

val_help: u32:0-2147483647 ; Expiry scan interval (default 60)
