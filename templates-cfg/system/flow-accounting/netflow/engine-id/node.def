type: u32
help: Set the engine-id
syntax:expression: $VAR(@) >= 0 && $VAR(@) <= 255; \
       "engine-id must be between 0-255"
comp_help: possible completions:
  <0-255>     Set engine-id (version 5 and 9 only)
